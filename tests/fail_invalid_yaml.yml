version: '3.8'

services:
test-service:  # Missing indentation - this will cause YAML parsing error
    image: alpine:latest
    container_name: test-container
    environment:
    - TEST_VAR=working
  command: echo "This YAML is invalid"
